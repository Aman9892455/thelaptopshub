
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TheLaptop Hub - Home</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: #f3f6fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .hero {
      background: linear-gradient(rgba(0,0,0,0.55), rgba(0,0,0,0.25)), url('https://images.unsplash.com/photo-1517336714731-489689fd1ca8?auto=format&fit=crop&w=1600&q=80') center/cover no-repeat;
      color: #fff;
      text-align: center;
      padding: 85px 16px 60px;
      border-radius: 0 0 30px 30px;
      margin-bottom: 0px;
    }
    .hero h1 {
      font-size: 2.7rem;
      font-weight: 700;
      margin-bottom: 10px;
      letter-spacing: 2px;
      text-shadow: 0 3px 16px rgba(0,0,0,0.18);
    }
    .hero p {
      font-size: 1rem;
      margin-bottom: 0;
      font-weight: 400;
      letter-spacing: 0.5px;
    }
    .laptop-card {
      border: none;
      border-radius: 14px;
      overflow: hidden;
      box-shadow: 0 3px 16px rgba(0,0,0,0.10);
      background: #fff;
      display: flex;
      flex-direction: column;
      height: 100%;
      transition: transform 0.15s;
    }
    .laptop-card:hover {
      transform: translateY(-6px) scale(1.02);
      box-shadow: 0 8px 32px rgba(0,0,0,0.12);
    }
    .laptop-image {
      position: relative;
      height: 195px;
      overflow: hidden;
      background: #f7f7f7;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .laptop-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.2s;
    }
    .laptop-card:hover .laptop-image img {
      transform: scale(1.07);
    }
    .laptop-badge {
      position: absolute;
      top: 9px;
      left: 9px;
      background: #1769aa;
      color: white;
      padding: 4px 11px;
      border-radius: 6px;
      font-size: 0.78rem;
      font-weight: 500;
      box-shadow: 0 2px 6px rgba(23,105,170,0.12);
      z-index: 1;
    }
    .laptop-content {
      padding: 15px 13px 11px 13px;
      flex: 1 1 auto;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
    }
    .card-title {
      font-size: 1.12rem;
      font-weight: 600;
      margin-bottom: 6px;
    }
    .laptop-price {
      font-size: 1.1rem;
      color: #219150;
      font-weight: bold;
      margin-bottom: 8px;
    }
    .spec-item {
      font-size: 0.92rem;
      margin-bottom: 4px;
      color: #444;
      display: flex;
      align-items: center;
    }
    .spec-item i {
      color: #1769aa;
      margin-right: 8px;
      font-size: 1rem;
      width: 18px;
      text-align: center;
    }
    .btn-view {
      background: #1769aa;
      color: #fff;
      margin-top: 10px;
      border-radius: 6px;
      font-size: 0.98rem;
      font-weight: 500;
      transition: background 0.18s;
      display: inline-block;
      padding: 9px 20px;
      border: none;
      text-decoration: none;
      text-align: center;
    }
    .btn-view:hover {
      background: #0b2c48;
      color: #fff;
      text-decoration: none;
    }
    @media (max-width: 991.98px) {
      .laptop-image {
        height: 150px;
      }
    }
    @media (max-width: 767.98px) {
      .hero {
        padding: 56px 7vw 32px 7vw;
        border-radius: 0 0 18px 18px;
      }
      .hero h1 {
        font-size: 1.8rem;
      }
      .laptop-image {
        height: 120px;
      }
      .laptop-content {
        padding: 10px 7px 7px 11px;
      }
      .spec-item {
        font-size: 0.88rem;
      }
      .btn-view {
        font-size: 0.93rem;
        padding: 8px 14px;
      }
    }
    @media (max-width: 575.98px) {
      .hero {
        padding: 27px 2vw 14px 2vw;
      }
      .hero h1 {
        font-size: 1.3rem;
      }
    }
  </style>
</head>
<body>
  <div class="hero">
    <h1>TheLaptop Hub</h1>
    <p>Find your perfect laptop for work, study, and gaming</p>
  </div>
  <section class="container my-5">
    <% if (laptops.length > 0) { %>
      <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        <% laptops.forEach(laptop => { %>
          <div class="col">
            <div class="laptop-card">
              <div class="laptop-image">
                <img src="<%= laptop.laptopImage %>" alt="<%= laptop.laptopName %>" 
                  onerror="this.src='https://via.placeholder.com/300x200?text=Image+Not+Found'">
                <div class="laptop-badge"><%= laptop.laptopCategory %></div>
              </div>
              <div class="laptop-content">
                <h5 class="card-title"><%= laptop.laptopName %></h5>
                <div class="text-muted mb-2"><%= laptop.laptopBrand %></div>
                <div class="laptop-price">$<%= laptop.laptopPrice ? laptop.laptopPrice.toLocaleString() : '' %></div>
                <div class="laptop-specs">
                  <div class="spec-item"><i class="fas fa-microchip"></i><span><%= laptop.laptopProcessor %></span></div>
                  <div class="spec-item"><i class="fas fa-memory"></i><span><%= laptop.laptopRAM %></span></div>
                  <div class="spec-item"><i class="fas fa-hdd"></i><span><%= laptop.laptopStorage %></span></div>
                  <div class="spec-item"><i class="fas fa-info-circle"></i><span><%= laptop.laptopDescription %></span></div>
                  <% if (laptop.laptopGraphics) { %>
                    <div class="spec-item"><i class="fas fa-video"></i><span><%= laptop.laptopGraphics %></span></div>
                  <% } %>
                  <div class="spec-item"><i class="fas fa-desktop"></i><span><%= laptop.laptopDisplay %></span></div>
                  <div class="spec-item"><i class="fas fa-laptop-code"></i><span><%= laptop.laptopOS %></span></div>
                </div>
                <a href="/getInTouch" class="btn btn-view">
              <i class="fas fa-eye me-2"></i>RENT NOW
               </a>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="text-center py-5">
        <h4>No laptops found</h4>
        <p>Please check back later for new arrivals.</p>
      </div>
    <% } %>
    <div class="text-center mt-5">
      <a href="/laptops" class="btn btn-primary btn-lg">View All Laptops <i class="fas fa-arrow-right ms-2"></i></a>
    </div>
  </section>
</body>
</html>
